enum Occupation { Jobholder, Business }

//project management setting
enum OccupationProject { Jobholder, Business }

//project management setting
enum Employee { Yes, No, secondDay }

enum Day { FullDay, SecondDay }

enum Half { FirstHalf, FullDay }

enum AttendanceEventType { attendanceIN, attendanceOUT }

enum Purpose { personal, official, end }

enum LocationType { office, known, others }
